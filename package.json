{
  "name": "javapages",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "next build && next export && rm -rf docs/* && cp -r out/* docs && cp CNAME docs && git add docs && git commit -m \"Update site\" && git push"
  },
  "dependencies": {
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "^8.3.0",
    "eslint-config-next": "11.1.2"
  }
}
